# Safe AI Session Backend

Production-oriented Fastify backend for multi-tenant AI session licensing, RBAC, reseller user generation, request-time subscription expiry, device locking, account leasing, usage reporting, and Redis-backed hot-path state.

Master account session material is encrypted at rest. During a successful authenticated lease, the browser extension receives the decrypted session material only long enough to inject the target workspace cookies into the local browser profile.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Copy environment template:

```bash
cp .env.example .env
```

3. Generate an RSA key pair and base64 encode the PEM values for `JWT_PRIVATE_KEY_BASE64` and `JWT_PUBLIC_KEY_BASE64`.

4. Generate a 32-byte encryption key for `COOKIE_ENCRYPTION_KEY_BASE64`.

5. Run Prisma:

```bash
npm run prisma:migrate
npm run seed
npm run build
npm start
```

For a full root-level integration checklist, see [SYSTEM_CHECKLIST.md](./SYSTEM_CHECKLIST.md).

## Endpoints

- `POST /auth/login`
- `POST /admin/create-admin`
- `POST /admin/generate-user`
- `POST /admin/plan-config`
- `POST /admin/app-config`
- `GET /admin/analytics`
- `GET /admin/users/active`
- `GET /admin/users/expired`
- `GET /admin/users/pending-manual`
- `POST /admin/user/toggle-status/:userId`
- `GET /client/config`
- `GET /session/lease-account`
- `POST /session/report-usage`
- `GET /health`

## Security Notes

- JWTs are signed with RS256.
- JWT `jti` values must exist in Redis or requests are rejected.
- Device fingerprint must match the JWT and registered device list.
- Master account cookies are encrypted at rest and are only returned through the authenticated `/session/lease-account` extension flow for the selected lease.
- Each master account is capped by `PROVIDER_INFLIGHT_JOB_CAPACITY` active in-flight jobs, with excess users receiving retryable lease responses.
- Subscription expiry is checked at request time in auth/session flows. No cron job is used.
- User `status` is computed in API responses from `validUntil`: `ACTIVE` when `validUntil > now`, otherwise `EXPIRED`.
- `isManuallyDisabled` is a hard lock override; session leasing rejects manually disabled users even when their subscription window is still valid.
- Admin user responses include `daysRemaining`, `systemExpired`, `manualDisable`, and `accessStatus` for dashboard filtering.
- Customer accounts must be generated by the system as `user_<id>@VIRTUAL_EMAIL_DOMAIN`.
- `/client/config` returns a plan/role-specific policy plus `configHash`, which is also bound into the JWT/session record.

## Low-resource VPS Notes

- Set `CLUSTER_WORKERS=1` or `2` on a 2GB VPS.
- Set `CLUSTER_WORKERS=2` to `4` on a 6GB VPS depending on database and Redis placement.
- `redis.low-resource.conf` uses no persistence and a bounded LRU cache profile for hot session/device keys.
- `ecosystem.config.cjs` runs one PM2 process and lets the app's internal cluster control worker count.

## Unified Build

Run the full backend, extension, and landing verification from the project root:

```bash
npm run build:all
```
